# 更新日志

本仓库自 `v1.5.0` 起的对外更新记录。最新发布版本：`v1.6.1`。

---

## v1.6.1 (2026-05-06)

### 修复
- macOS：使用 ad-hoc 自签名解决「应用已损坏」拦截，文档同步更新「右键打开」绕过 Gatekeeper 的引导。
- Windows：捕获文本时彻底隐藏 PowerShell 黑窗；NSIS 安装器允许提权关闭旧进程，覆盖安装更稳。
- 结果窗口尺寸与排版优化，⊕ 插入标记加入动画、放大与背景色，长文段查看更醒目。
- 选中触发条件收紧：长度 ≥4 字、非中日韩字符 ≥10，规避输入法、菜单和提示语误触发。

### 体验
- 主页、设置、引导合并到统一的 5 Tab 设置窗口（首页 Tab 含引导），窗口可自由调节大小。
- 关闭设置/引导窗口后自动隐藏 Dock 图标。
- 「确认选用」按钮：可仅采纳部分建议后整体替换原文。
- 防御弱模型返回「建议」而非改写文本，加强 sanitize 与提示词。

### 商业版（新增）
- 新增本地可运行的 Node.js + SQLite 后端（`server/`）：账户、设备绑定、积分、套餐、签到、调用记录。
- 接入硅基流动作为默认上游，预留微信支付 / 支付宝 / 爱发卡等通道（默认手动开单）。
- 中间件齐全：限流（设备/全局 QPS）、熔断器、成本守护（日/月预算 + 自动降级）、请求队列。
- 桌面端「润石会员」：登录、套餐购买、积分余额查看、设备管理。
- 模型分级计费：基础 / 标准 / 高级 / 旗舰，倍率写在配置里方便调整。
- 新增模型：DeepSeek-V4-Flash（高级，2 积分）、Kimi-K2.6 / Kimi-K2.6 Pro（标准，1 积分），均带「新」徽章。

### 文档与官网
- 官网 `docs/` 全面重写：首页、技术报告页、用户协议、`version.json`、CNAME；标语统一为「润色构石，构石虽糙，一润成文」。
- 自动构建脚本：`scripts/build-site-dist.sh`、`scripts/deploy-pages.sh`，配合 `.github/workflows/deploy-pages.yml` 自动部署到 GitHub Pages。
- 新增 [TECHNICAL_REPORT.md](TECHNICAL_REPORT.md)、[COMMERCIAL_SETUP.md](COMMERCIAL_SETUP.md) 两份对外说明。

### 测试
- 新增/扩充测试：熔断器、积分策略、套餐方案、模型超时、支付流、商业 Provider 归一化、updater，共 30+ 用例。
- 测试入口统一为 `node --test`。

---

## v1.6.0 (2026-04-下旬)

- 商业后端首版上线，含账户 / 设备绑定 / 计费 / 套餐购买全链路。
- 新增「自动更新」模块（`src/updater/`），可在「关于」页手动检查或自动后台检查。
- 选中助手 Swift 探针 `scripts/selection_probe.swift` 重写，macOS 选中识别更稳。
- 默认快捷键调整、托盘菜单整理、首次启动引导优化。

---

## v1.5.0 (2026-04-中旬)

- 移除 macOS 专属描述，桌面端正式跨平台（macOS + Windows）。
- 设置页重构为 5 Tab 结构，「通用」与「提示词」独立。
- 关于页加入作者署名 `Choras Chan` 与新版标语。
- 主页窗口加入官网 slogan，启动 / 二次实例 / 托盘均可打开。

---

## 历史版本

历史更新见 git log。`v1.4.0` 起完成 macOS + Windows 跨平台支持；`v1.4.0` 之前为 macOS 专属早期版本。

---

## 不在仓库内的内容

下列材料属于作者的个人/比赛/对外稿件，已在 [.gitignore](.gitignore) 显式排除，不会随源码发布：

- `docs/*.docx`、`docs/*.pdf`：白皮书、参赛说明、演示稿等私人文件
- `docs/gen_whitepaper.py`：上述文档的生成脚本
- `docs/downloads/`：安装包分发目录
- `data/`、`.env`：本地数据库与环境密钥
- `.publish/`：本地发布工作区
