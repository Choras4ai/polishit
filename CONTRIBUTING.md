# Contributing

感谢参与润石 PoliShit。

## 提交前检查

请先确认：

1. 变更范围尽量聚焦，不顺手夹带无关重构
2. 保留现有中英文产品命名：`润石 PoliShit`
3. 涉及功能文案时，`降AI率（图一乐版）` 的括号内容需要完整保留
4. 提交前运行 `npm test`

## Bug 报告建议

浮窗、复制、快捷键类问题请尽量提供：

- 操作系统及版本（macOS / Windows）
- 应用版本
- 目标应用名称（例如 Chrome、Word、WPS、Obsidian）
- 是“选中不弹”还是“复制不弹”
- 复现步骤和预期结果

## Pull Request 建议

- 修改 UI 时附截图
- 修改主进程或监听链路时写明影响范围
- 新增配置项时同步更新 README
- 不要在 PR 中提交构建产物 `dist/`

## 代码风格

- 优先沿用现有项目结构和命名
- 默认使用 ASCII，除非用户可见文案确实需要中文
- 注释只写必要背景，不写逐行复述
